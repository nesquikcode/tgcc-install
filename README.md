# Установка (Termux)
```bash
  pkg update && pkg upgrade -y && pkg install curl -y && curl https://raw.githubusercontent.com/nesquikcode/tgcc-install/refs/heads/main/termux-install.sh --output termux-install.sh && clear && sh termux-install.sh
```
